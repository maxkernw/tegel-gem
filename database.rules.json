{
  "rules": {
    "events": {
      ".read": "auth != null",
      "$event_id": {
        ".write": "auth != null && ((!data.exists() && newData.child('userId').val() === auth.uid) || (data.exists() && data.child('userId').val() === auth.uid))",
        ".validate": "newData.hasChildren(['userId', 'title', 'start', 'end', 'email'])"
      }
    },
    "chalkboard": {
      ".read": "auth != null",
      ".write": "auth != null && !newData.exists()",
      "$line_id": {
        ".write": "auth != null && ((!data.exists() && newData.child('userId').val() === auth.uid) || (data.exists() && data.child('userId').val() === auth.uid))"
      }
    }
  }
}

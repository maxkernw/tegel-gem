import { defineConfig } from 'vite'

export default defineConfig({
  // Replace 'tegel-gems' with your actual repository name if it's different
  base: '/tegel-gems/',
  build: {
    outDir: 'dist',
  }
})
